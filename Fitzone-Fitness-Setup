import React from "react";
import "./FitnessSetup.css";
import { Link } from "react-router-dom";

export default function FitnessSetup() {
    return (
      <div className="fitness-container">
        {/* Back to home */}
        
        <Link to="/start" className="back-link">← Back to Home</Link>
  
        {/* Heading */}
        <h1 className="page-title">
          Start <span>Your Fitness Journey</span>
        </h1>
        <p className="subtitle">
          Tell us about yourself to create your personalized fitness profile
        </p>
  
        {/* Card */}
        <div className="form-card">
          <h2 className="form-title">Personal Information</h2>
          <p className="form-subtitle">
            Help us customize your fitness experience
          </p>
  
          <form>
            {/* Name + Age */}
            <div className="form-row">
              <div className="form-group">
                <label>Full Name</label>
                <input type="text" placeholder="Enter your full name" />
              </div>
              <div className="form-group">
                <label>Age</label>
                <input type="number" placeholder="Enter your age" />
              </div>
            </div>
  
            {/* Workout Type */}
            <div className="form-group">
              <label>Preferred Workout Type</label>
              <select>
                <option>Select your preferred workout</option>
                <option>Strength Training</option>
                <option>Cardio</option>
                <option>Yoga</option>
                <option>CrossFit</option>
              </select>
            </div>
  
            {/* Max Heart Rate */}
            <div className="form-group">
              <label>Max Bead </label>
              <input type="number" placeholder="Rs500" />
              <small>
               <p>This is more than a number, it’s your step toward transformation. Set your max budget!</p>
              </small>
            </div>
  
            {/* File Upload */}
            <div className="form-group">
              <label>Upload Photo or Video</label>
              <div className="upload-box">
                <span>⬆</span>
                <p>
                  Click to upload or drag and drop <br />
                  <small>PNG, JPG, MP4 up to 10MB</small>
                </p>
              </div>
            </div>
  
            {/* Button */}
            <button type="submit" className="btn-submit">
            <Link to="/requestbead">Complete Setup & Start Training</Link>          
            </button>
          </form>
        </div>
      </div>
    );
  }
